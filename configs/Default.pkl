/*
  Default configuration with tools that work for most repos. Don't autofix on pre-commit.
*/
amends "../Config.pkl"
import "../Functions.pkl"
import "../steps/Default.pkl" as Steps

hooks = Functions.defaultHooks(false, Steps.steps)
