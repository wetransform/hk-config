/*
  Default configuration but assumes OpenTofu is used instead of Terraform in case there are Terraform related files. Don't autofix on pre-commit.
*/
amends "../Config.pkl"
import "../Functions.pkl"
import "../steps/Tofu.pkl" as Steps

hooks = Functions.defaultHooks(false, Steps.steps)
